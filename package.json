{
  "name": "weather_app",
  "version": "1.0.0",
  "description": "this is weather app tell the real data from an api",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "shubham",
  "license": "ISC",
  "dependencies": {
    "requests": "^0.3.0"
  }
}
